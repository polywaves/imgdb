services:
  mongo:
    image: mongo
    command: mongod --replSet rs0
    restart: always
    env_file:
      - .env
    ports:
      - 27337:27017
    volumes:
      - ~/data/mongo:/data

  mongo1:
    image: mongo
    command: mongod --replSet rs0
    restart: always
    env_file:
      - .env
    volumes:
      - ~/data/mongo1:/data

  mongo2:
    image: mongo
    command: mongod --replSet rs0
    restart: always
    env_file:
      - .env
    volumes:
      - ~/data/mongo2:/data